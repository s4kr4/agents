# ~/.agents リポジトリガイド

## エージェント構成の見直し方針

多段フェーズ（調査→計画→TDD→検証）は「導入当時のモデルが単独ではこなせなかったこと」を
補う構造であり、モデルを更新するたびに前提が変わる。定期的に構成を再評価する。

### 見直しのトリガー

- 使用モデルをアップグレードしたとき
- あるフェーズの出力が素通しに近い（指摘ゼロ・修正ゼロ）状態が続いたとき

### フェーズの分類

見直し時は、各フェーズの存在意義がどちらかを先に判定する：

- **能力の補完**（例: 事前調査、計画立案）→ モデル更新で不要になりうる。削減候補
- **検証の独立性**（例: tester/implementer 分離、独立レビュー）→ モデルの賢さと無関係に
  価値が残る。原則維持

### 削減の実験手順

1. 削減候補を1つだけ選ぶ（複数フェーズを同時に外さない）
2. そのフェーズをスキップした構成で、普段の代表的なタスクを2〜3件実施する
3. 品質を比較する（検証フェーズの指摘件数、差し戻し回数、手戻りの有無）
4. 劣化がなければ削減を確定し `development-workflow.md` を更新、劣化があれば戻す

### 追加方向の確認

削減だけでなく、新モデルで新たに自動化可能になったこと（これまで諦めていた検証・
これまで手動だった作業）がないかも同時に確認する。
