---
description: TDD（テスト駆動開発）ルール。コード実装を伴うすべての作業に適用。
paths:
  - "**/*.ts"
  - "**/*.tsx"
  - "**/*.js"
  - "**/*.jsx"
  - "**/*.py"
---

# TDD ルール

## 原則

**コードを実装する際は、必ずテストを先に書くこと。**

プロダクションコードの実装より前にテストが存在しない場合、その実装は TDD に違反している。

## Red-Green-Refactor サイクル

すべての実装はこのサイクルに従うこと:

1. **Red**: 失敗するテストを書く
2. **Green**: テストが通る最小限のコードを書く
3. **Refactor**: テストを通したままコードを整理する

このサイクルを1テストケースずつ繰り返す。複数のテストをまとめて書いてから実装するのは禁止。

## オーケストレーション時の例外

以下のいずれかに該当する場合、tester / implementer 分離ワークフロー（`development-workflow.md` の Phase 3a/3b、`/tdd` スキルのオーケストレーション手順）の一部として動作しており、この例外が適用される:

- 自分が tester / implementer 専任のエージェント定義（`@web-api-tester` / `@web-ui-tester` / `@web-api-implementer` / `@web-ui-implementer`）で起動されている
- 委譲プロンプトに RED / GREEN フェーズの明示がある

例外の内容:

- tester: RED フェーズとして、スコープ内の全テストケースを一括作成してよい（TDD 違反ではない）
- implementer: 受領した failing テストを1つずつ GREEN にする（「1サイクルずつ」の原則は実装順序に適用する）

上記のいずれにも該当しない場合（メインセッションや汎用エージェントがテスト作成と実装の両方を担う場合）は、従来どおり1テストケースずつのサイクルで進める。

## 禁止事項

- プロダクションコードを先に書き、後からテストを追加する
- テストなしでプロダクションコードを実装する
- 「シンプルな変更だからテスト不要」と自己判断してテストを省略する
- すべてのテストケースをまとめて書いてから実装する（1サイクルずつ進めること。ただしオーケストレーション時の例外を参照）

## テストケースの設計順序

1. 正常系（ハッピーパス）
2. 境界値
3. エラーケース・異常系

## 参照スキル

詳細な手順は `/tdd` スキルを参照すること。
